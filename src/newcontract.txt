/**
 *Submitted for verification at polygonscan.com on 2025-12-05
*/

// File: @openzeppelin/contracts/token/ERC20/IERC20.sol


// OpenZeppelin Contracts (last updated v5.4.0) (token/ERC20/IERC20.sol)

pragma solidity >=0.4.16;

/**
 * @dev Interface of the ERC-20 standard as defined in the ERC.
 */
interface IERC20 {
    /**
     * @dev Emitted when `value` tokens are moved from one account (`from`) to
     * another (`to`).
     *
     * Note that `value` may be zero.
     */
    event Transfer(address indexed from, address indexed to, uint256 value);

    /**
     * @dev Emitted when the allowance of a `spender` for an `owner` is set by
     * a call to {approve}. `value` is the new allowance.
     */
    event Approval(address indexed owner, address indexed spender, uint256 value);

    /**
     * @dev Returns the value of tokens in existence.
     */
    function totalSupply() external view returns (uint256);

    /**
     * @dev Returns the value of tokens owned by `account`.
     */
    function balanceOf(address account) external view returns (uint256);

    /**
     * @dev Moves a `value` amount of tokens from the caller's account to `to`.
     *
     * Returns a boolean value indicating whether the operation succeeded.
     *
     * Emits a {Transfer} event.
     */
    function transfer(address to, uint256 value) external returns (bool);

    /**
     * @dev Returns the remaining number of tokens that `spender` will be
     * allowed to spend on behalf of `owner` through {transferFrom}. This is
     * zero by default.
     *
     * This value changes when {approve} or {transferFrom} are called.
     */
    function allowance(address owner, address spender) external view returns (uint256);

    /**
     * @dev Sets a `value` amount of tokens as the allowance of `spender` over the
     * caller's tokens.
     *
     * Returns a boolean value indicating whether the operation succeeded.
     *
     * IMPORTANT: Beware that changing an allowance with this method brings the risk
     * that someone may use both the old and the new allowance by unfortunate
     * transaction ordering. One possible solution to mitigate this race
     * condition is to first reduce the spender's allowance to 0 and set the
     * desired value afterwards:
     * https://github.com/ethereum/EIPs/issues/20#issuecomment-263524729
     *
     * Emits an {Approval} event.
     */
    function approve(address spender, uint256 value) external returns (bool);

    /**
     * @dev Moves a `value` amount of tokens from `from` to `to` using the
     * allowance mechanism. `value` is then deducted from the caller's
     * allowance.
     *
     * Returns a boolean value indicating whether the operation succeeded.
     *
     * Emits a {Transfer} event.
     */
    function transferFrom(address from, address to, uint256 value) external returns (bool);
}

// File: @openzeppelin/contracts/token/ERC20/extensions/IERC20Metadata.sol


// OpenZeppelin Contracts (last updated v5.4.0) (token/ERC20/extensions/IERC20Metadata.sol)

pragma solidity >=0.6.2;


/**
 * @dev Interface for the optional metadata functions from the ERC-20 standard.
 */
interface IERC20Metadata is IERC20 {
    /**
     * @dev Returns the name of the token.
     */
    function name() external view returns (string memory);

    /**
     * @dev Returns the symbol of the token.
     */
    function symbol() external view returns (string memory);

    /**
     * @dev Returns the decimals places of the token.
     */
    function decimals() external view returns (uint8);
}

// File: @openzeppelin/contracts/utils/Context.sol


// OpenZeppelin Contracts (last updated v5.0.1) (utils/Context.sol)

pragma solidity ^0.8.20;

/**
 * @dev Provides information about the current execution context, including the
 * sender of the transaction and its data. While these are generally available
 * via msg.sender and msg.data, they should not be accessed in such a direct
 * manner, since when dealing with meta-transactions the account sending and
 * paying for execution may not be the actual sender (as far as an application
 * is concerned).
 *
 * This contract is only required for intermediate, library-like contracts.
 */
abstract contract Context {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }

    function _msgData() internal view virtual returns (bytes calldata) {
        return msg.data;
    }

    function _contextSuffixLength() internal view virtual returns (uint256) {
        return 0;
    }
}

// File: @openzeppelin/contracts/interfaces/draft-IERC6093.sol


// OpenZeppelin Contracts (last updated v5.4.0) (interfaces/draft-IERC6093.sol)
pragma solidity >=0.8.4;

/**
 * @dev Standard ERC-20 Errors
 * Interface of the https://eips.ethereum.org/EIPS/eip-6093[ERC-6093] custom errors for ERC-20 tokens.
 */
interface IERC20Errors {
    /**
     * @dev Indicates an error related to the current `balance` of a `sender`. Used in transfers.
     * @param sender Address whose tokens are being transferred.
     * @param balance Current balance for the interacting account.
     * @param needed Minimum amount required to perform a transfer.
     */
    error ERC20InsufficientBalance(address sender, uint256 balance, uint256 needed);

    /**
     * @dev Indicates a failure with the token `sender`. Used in transfers.
     * @param sender Address whose tokens are being transferred.
     */
    error ERC20InvalidSender(address sender);

    /**
     * @dev Indicates a failure with the token `receiver`. Used in transfers.
     * @param receiver Address to which tokens are being transferred.
     */
    error ERC20InvalidReceiver(address receiver);

    /**
     * @dev Indicates a failure with the `spender`’s `allowance`. Used in transfers.
     * @param spender Address that may be allowed to operate on tokens without being their owner.
     * @param allowance Amount of tokens a `spender` is allowed to operate with.
     * @param needed Minimum amount required to perform a transfer.
     */
    error ERC20InsufficientAllowance(address spender, uint256 allowance, uint256 needed);

    /**
     * @dev Indicates a failure with the `approver` of a token to be approved. Used in approvals.
     * @param approver Address initiating an approval operation.
     */
    error ERC20InvalidApprover(address approver);

    /**
     * @dev Indicates a failure with the `spender` to be approved. Used in approvals.
     * @param spender Address that may be allowed to operate on tokens without being their owner.
     */
    error ERC20InvalidSpender(address spender);
}

/**
 * @dev Standard ERC-721 Errors
 * Interface of the https://eips.ethereum.org/EIPS/eip-6093[ERC-6093] custom errors for ERC-721 tokens.
 */
interface IERC721Errors {
    /**
     * @dev Indicates that an address can't be an owner. For example, `address(0)` is a forbidden owner in ERC-20.
     * Used in balance queries.
     * @param owner Address of the current owner of a token.
     */
    error ERC721InvalidOwner(address owner);

    /**
     * @dev Indicates a `tokenId` whose `owner` is the zero address.
     * @param tokenId Identifier number of a token.
     */
    error ERC721NonexistentToken(uint256 tokenId);

    /**
     * @dev Indicates an error related to the ownership over a particular token. Used in transfers.
     * @param sender Address whose tokens are being transferred.
     * @param tokenId Identifier number of a token.
     * @param owner Address of the current owner of a token.
     */
    error ERC721IncorrectOwner(address sender, uint256 tokenId, address owner);

    /**
     * @dev Indicates a failure with the token `sender`. Used in transfers.
     * @param sender Address whose tokens are being transferred.
     */
    error ERC721InvalidSender(address sender);

    /**
     * @dev Indicates a failure with the token `receiver`. Used in transfers.
     * @param receiver Address to which tokens are being transferred.
     */
    error ERC721InvalidReceiver(address receiver);

    /**
     * @dev Indicates a failure with the `operator`’s approval. Used in transfers.
     * @param operator Address that may be allowed to operate on tokens without being their owner.
     * @param tokenId Identifier number of a token.
     */
    error ERC721InsufficientApproval(address operator, uint256 tokenId);

    /**
     * @dev Indicates a failure with the `approver` of a token to be approved. Used in approvals.
     * @param approver Address initiating an approval operation.
     */
    error ERC721InvalidApprover(address approver);

    /**
     * @dev Indicates a failure with the `operator` to be approved. Used in approvals.
     * @param operator Address that may be allowed to operate on tokens without being their owner.
     */
    error ERC721InvalidOperator(address operator);
}

/**
 * @dev Standard ERC-1155 Errors
 * Interface of the https://eips.ethereum.org/EIPS/eip-6093[ERC-6093] custom errors for ERC-1155 tokens.
 */
interface IERC1155Errors {
    /**
     * @dev Indicates an error related to the current `balance` of a `sender`. Used in transfers.
     * @param sender Address whose tokens are being transferred.
     * @param balance Current balance for the interacting account.
     * @param needed Minimum amount required to perform a transfer.
     * @param tokenId Identifier number of a token.
     */
    error ERC1155InsufficientBalance(address sender, uint256 balance, uint256 needed, uint256 tokenId);

    /**
     * @dev Indicates a failure with the token `sender`. Used in transfers.
     * @param sender Address whose tokens are being transferred.
     */
    error ERC1155InvalidSender(address sender);

    /**
     * @dev Indicates a failure with the token `receiver`. Used in transfers.
     * @param receiver Address to which tokens are being transferred.
     */
    error ERC1155InvalidReceiver(address receiver);

    /**
     * @dev Indicates a failure with the `operator`’s approval. Used in transfers.
     * @param operator Address that may be allowed to operate on tokens without being their owner.
     * @param owner Address of the current owner of a token.
     */
    error ERC1155MissingApprovalForAll(address operator, address owner);

    /**
     * @dev Indicates a failure with the `approver` of a token to be approved. Used in approvals.
     * @param approver Address initiating an approval operation.
     */
    error ERC1155InvalidApprover(address approver);

    /**
     * @dev Indicates a failure with the `operator` to be approved. Used in approvals.
     * @param operator Address that may be allowed to operate on tokens without being their owner.
     */
    error ERC1155InvalidOperator(address operator);

    /**
     * @dev Indicates an array length mismatch between ids and values in a safeBatchTransferFrom operation.
     * Used in batch transfers.
     * @param idsLength Length of the array of token identifiers
     * @param valuesLength Length of the array of token amounts
     */
    error ERC1155InvalidArrayLength(uint256 idsLength, uint256 valuesLength);
}

// File: @openzeppelin/contracts/token/ERC20/ERC20.sol


// OpenZeppelin Contracts (last updated v5.4.0) (token/ERC20/ERC20.sol)

pragma solidity ^0.8.20;





/**
 * @dev Implementation of the {IERC20} interface.
 *
 * This implementation is agnostic to the way tokens are created. This means
 * that a supply mechanism has to be added in a derived contract using {_mint}.
 *
 * TIP: For a detailed writeup see our guide
 * https://forum.openzeppelin.com/t/how-to-implement-erc20-supply-mechanisms/226[How
 * to implement supply mechanisms].
 *
 * The default value of {decimals} is 18. To change this, you should override
 * this function so it returns a different value.
 *
 * We have followed general OpenZeppelin Contracts guidelines: functions revert
 * instead returning `false` on failure. This behavior is nonetheless
 * conventional and does not conflict with the expectations of ERC-20
 * applications.
 */
abstract contract ERC20 is Context, IERC20, IERC20Metadata, IERC20Errors {
    mapping(address account => uint256) private _balances;

    mapping(address account => mapping(address spender => uint256)) private _allowances;

    uint256 private _totalSupply;

    string private _name;
    string private _symbol;

    /**
     * @dev Sets the values for {name} and {symbol}.
     *
     * Both values are immutable: they can only be set once during construction.
     */
    constructor(string memory name_, string memory symbol_) {
        _name = name_;
        _symbol = symbol_;
    }

    /**
     * @dev Returns the name of the token.
     */
    function name() public view virtual returns (string memory) {
        return _name;
    }

    /**
     * @dev Returns the symbol of the token, usually a shorter version of the
     * name.
     */
    function symbol() public view virtual returns (string memory) {
        return _symbol;
    }

    /**
     * @dev Returns the number of decimals used to get its user representation.
     * For example, if `decimals` equals `2`, a balance of `505` tokens should
     * be displayed to a user as `5.05` (`505 / 10 ** 2`).
     *
     * Tokens usually opt for a value of 18, imitating the relationship between
     * Ether and Wei. This is the default value returned by this function, unless
     * it's overridden.
     *
     * NOTE: This information is only used for _display_ purposes: it in
     * no way affects any of the arithmetic of the contract, including
     * {IERC20-balanceOf} and {IERC20-transfer}.
     */
    function decimals() public view virtual returns (uint8) {
        return 18;
    }

    /// @inheritdoc IERC20
    function totalSupply() public view virtual returns (uint256) {
        return _totalSupply;
    }

    /// @inheritdoc IERC20
    function balanceOf(address account) public view virtual returns (uint256) {
        return _balances[account];
    }

    /**
     * @dev See {IERC20-transfer}.
     *
     * Requirements:
     *
     * - `to` cannot be the zero address.
     * - the caller must have a balance of at least `value`.
     */
    function transfer(address to, uint256 value) public virtual returns (bool) {
        address owner = _msgSender();
        _transfer(owner, to, value);
        return true;
    }

    /// @inheritdoc IERC20
    function allowance(address owner, address spender) public view virtual returns (uint256) {
        return _allowances[owner][spender];
    }

    /**
     * @dev See {IERC20-approve}.
     *
     * NOTE: If `value` is the maximum `uint256`, the allowance is not updated on
     * `transferFrom`. This is semantically equivalent to an infinite approval.
     *
     * Requirements:
     *
     * - `spender` cannot be the zero address.
     */
    function approve(address spender, uint256 value) public virtual returns (bool) {
        address owner = _msgSender();
        _approve(owner, spender, value);
        return true;
    }

    /**
     * @dev See {IERC20-transferFrom}.
     *
     * Skips emitting an {Approval} event indicating an allowance update. This is not
     * required by the ERC. See {xref-ERC20-_approve-address-address-uint256-bool-}[_approve].
     *
     * NOTE: Does not update the allowance if the current allowance
     * is the maximum `uint256`.
     *
     * Requirements:
     *
     * - `from` and `to` cannot be the zero address.
     * - `from` must have a balance of at least `value`.
     * - the caller must have allowance for ``from``'s tokens of at least
     * `value`.
     */
    function transferFrom(address from, address to, uint256 value) public virtual returns (bool) {
        address spender = _msgSender();
        _spendAllowance(from, spender, value);
        _transfer(from, to, value);
        return true;
    }

    /**
     * @dev Moves a `value` amount of tokens from `from` to `to`.
     *
     * This internal function is equivalent to {transfer}, and can be used to
     * e.g. implement automatic token fees, slashing mechanisms, etc.
     *
     * Emits a {Transfer} event.
     *
     * NOTE: This function is not virtual, {_update} should be overridden instead.
     */
    function _transfer(address from, address to, uint256 value) internal {
        if (from == address(0)) {
            revert ERC20InvalidSender(address(0));
        }
        if (to == address(0)) {
            revert ERC20InvalidReceiver(address(0));
        }
        _update(from, to, value);
    }

    /**
     * @dev Transfers a `value` amount of tokens from `from` to `to`, or alternatively mints (or burns) if `from`
     * (or `to`) is the zero address. All customizations to transfers, mints, and burns should be done by overriding
     * this function.
     *
     * Emits a {Transfer} event.
     */
    function _update(address from, address to, uint256 value) internal virtual {
        if (from == address(0)) {
            // Overflow check required: The rest of the code assumes that totalSupply never overflows
            _totalSupply += value;
        } else {
            uint256 fromBalance = _balances[from];
            if (fromBalance < value) {
                revert ERC20InsufficientBalance(from, fromBalance, value);
            }
            unchecked {
                // Overflow not possible: value <= fromBalance <= totalSupply.
                _balances[from] = fromBalance - value;
            }
        }

        if (to == address(0)) {
            unchecked {
                // Overflow not possible: value <= totalSupply or value <= fromBalance <= totalSupply.
                _totalSupply -= value;
            }
        } else {
            unchecked {
                // Overflow not possible: balance + value is at most totalSupply, which we know fits into a uint256.
                _balances[to] += value;
            }
        }

        emit Transfer(from, to, value);
    }

    /**
     * @dev Creates a `value` amount of tokens and assigns them to `account`, by transferring it from address(0).
     * Relies on the `_update` mechanism
     *
     * Emits a {Transfer} event with `from` set to the zero address.
     *
     * NOTE: This function is not virtual, {_update} should be overridden instead.
     */
    function _mint(address account, uint256 value) internal {
        if (account == address(0)) {
            revert ERC20InvalidReceiver(address(0));
        }
        _update(address(0), account, value);
    }

    /**
     * @dev Destroys a `value` amount of tokens from `account`, lowering the total supply.
     * Relies on the `_update` mechanism.
     *
     * Emits a {Transfer} event with `to` set to the zero address.
     *
     * NOTE: This function is not virtual, {_update} should be overridden instead
     */
    function _burn(address account, uint256 value) internal {
        if (account == address(0)) {
            revert ERC20InvalidSender(address(0));
        }
        _update(account, address(0), value);
    }

    /**
     * @dev Sets `value` as the allowance of `spender` over the `owner`'s tokens.
     *
     * This internal function is equivalent to `approve`, and can be used to
     * e.g. set automatic allowances for certain subsystems, etc.
     *
     * Emits an {Approval} event.
     *
     * Requirements:
     *
     * - `owner` cannot be the zero address.
     * - `spender` cannot be the zero address.
     *
     * Overrides to this logic should be done to the variant with an additional `bool emitEvent` argument.
     */
    function _approve(address owner, address spender, uint256 value) internal {
        _approve(owner, spender, value, true);
    }

    /**
     * @dev Variant of {_approve} with an optional flag to enable or disable the {Approval} event.
     *
     * By default (when calling {_approve}) the flag is set to true. On the other hand, approval changes made by
     * `_spendAllowance` during the `transferFrom` operation set the flag to false. This saves gas by not emitting any
     * `Approval` event during `transferFrom` operations.
     *
     * Anyone who wishes to continue emitting `Approval` events on the`transferFrom` operation can force the flag to
     * true using the following override:
     *
     * ```solidity
     * function _approve(address owner, address spender, uint256 value, bool) internal virtual override {
     *     super._approve(owner, spender, value, true);
     * }
     * ```
     *
     * Requirements are the same as {_approve}.
     */
    function _approve(address owner, address spender, uint256 value, bool emitEvent) internal virtual {
        if (owner == address(0)) {
            revert ERC20InvalidApprover(address(0));
        }
        if (spender == address(0)) {
            revert ERC20InvalidSpender(address(0));
        }
        _allowances[owner][spender] = value;
        if (emitEvent) {
            emit Approval(owner, spender, value);
        }
    }

    /**
     * @dev Updates `owner`'s allowance for `spender` based on spent `value`.
     *
     * Does not update the allowance value in case of infinite allowance.
     * Revert if not enough allowance is available.
     *
     * Does not emit an {Approval} event.
     */
    function _spendAllowance(address owner, address spender, uint256 value) internal virtual {
        uint256 currentAllowance = allowance(owner, spender);
        if (currentAllowance < type(uint256).max) {
            if (currentAllowance < value) {
                revert ERC20InsufficientAllowance(spender, currentAllowance, value);
            }
            unchecked {
                _approve(owner, spender, currentAllowance - value, false);
            }
        }
    }
}

// File: @openzeppelin/contracts/access/Ownable.sol


// OpenZeppelin Contracts (last updated v5.0.0) (access/Ownable.sol)

pragma solidity ^0.8.20;


/**
 * @dev Contract module which provides a basic access control mechanism, where
 * there is an account (an owner) that can be granted exclusive access to
 * specific functions.
 *
 * The initial owner is set to the address provided by the deployer. This can
 * later be changed with {transferOwnership}.
 *
 * This module is used through inheritance. It will make available the modifier
 * `onlyOwner`, which can be applied to your functions to restrict their use to
 * the owner.
 */
abstract contract Ownable is Context {
    address private _owner;

    /**
     * @dev The caller account is not authorized to perform an operation.
     */
    error OwnableUnauthorizedAccount(address account);

    /**
     * @dev The owner is not a valid owner account. (eg. `address(0)`)
     */
    error OwnableInvalidOwner(address owner);

    event OwnershipTransferred(address indexed previousOwner, address indexed newOwner);

    /**
     * @dev Initializes the contract setting the address provided by the deployer as the initial owner.
     */
    constructor(address initialOwner) {
        if (initialOwner == address(0)) {
            revert OwnableInvalidOwner(address(0));
        }
        _transferOwnership(initialOwner);
    }

    /**
     * @dev Throws if called by any account other than the owner.
     */
    modifier onlyOwner() {
        _checkOwner();
        _;
    }

    /**
     * @dev Returns the address of the current owner.
     */
    function owner() public view virtual returns (address) {
        return _owner;
    }

    /**
     * @dev Throws if the sender is not the owner.
     */
    function _checkOwner() internal view virtual {
        if (owner() != _msgSender()) {
            revert OwnableUnauthorizedAccount(_msgSender());
        }
    }

    /**
     * @dev Leaves the contract without owner. It will not be possible to call
     * `onlyOwner` functions. Can only be called by the current owner.
     *
     * NOTE: Renouncing ownership will leave the contract without an owner,
     * thereby disabling any functionality that is only available to the owner.
     */
    function renounceOwnership() public virtual onlyOwner {
        _transferOwnership(address(0));
    }

    /**
     * @dev Transfers ownership of the contract to a new account (`newOwner`).
     * Can only be called by the current owner.
     */
    function transferOwnership(address newOwner) public virtual onlyOwner {
        if (newOwner == address(0)) {
            revert OwnableInvalidOwner(address(0));
        }
        _transferOwnership(newOwner);
    }

    /**
     * @dev Transfers ownership of the contract to a new account (`newOwner`).
     * Internal function without access restriction.
     */
    function _transferOwnership(address newOwner) internal virtual {
        address oldOwner = _owner;
        _owner = newOwner;
        emit OwnershipTransferred(oldOwner, newOwner);
    }
}

// File: @openzeppelin/contracts/access/IAccessControl.sol


// OpenZeppelin Contracts (last updated v5.4.0) (access/IAccessControl.sol)

pragma solidity >=0.8.4;

/**
 * @dev External interface of AccessControl declared to support ERC-165 detection.
 */
interface IAccessControl {
    /**
     * @dev The `account` is missing a role.
     */
    error AccessControlUnauthorizedAccount(address account, bytes32 neededRole);

    /**
     * @dev The caller of a function is not the expected one.
     *
     * NOTE: Don't confuse with {AccessControlUnauthorizedAccount}.
     */
    error AccessControlBadConfirmation();

    /**
     * @dev Emitted when `newAdminRole` is set as ``role``'s admin role, replacing `previousAdminRole`
     *
     * `DEFAULT_ADMIN_ROLE` is the starting admin for all roles, despite
     * {RoleAdminChanged} not being emitted to signal this.
     */
    event RoleAdminChanged(bytes32 indexed role, bytes32 indexed previousAdminRole, bytes32 indexed newAdminRole);

    /**
     * @dev Emitted when `account` is granted `role`.
     *
     * `sender` is the account that originated the contract call. This account bears the admin role (for the granted role).
     * Expected in cases where the role was granted using the internal {AccessControl-_grantRole}.
     */
    event RoleGranted(bytes32 indexed role, address indexed account, address indexed sender);

    /**
     * @dev Emitted when `account` is revoked `role`.
     *
     * `sender` is the account that originated the contract call:
     *   - if using `revokeRole`, it is the admin role bearer
     *   - if using `renounceRole`, it is the role bearer (i.e. `account`)
     */
    event RoleRevoked(bytes32 indexed role, address indexed account, address indexed sender);

    /**
     * @dev Returns `true` if `account` has been granted `role`.
     */
    function hasRole(bytes32 role, address account) external view returns (bool);

    /**
     * @dev Returns the admin role that controls `role`. See {grantRole} and
     * {revokeRole}.
     *
     * To change a role's admin, use {AccessControl-_setRoleAdmin}.
     */
    function getRoleAdmin(bytes32 role) external view returns (bytes32);

    /**
     * @dev Grants `role` to `account`.
     *
     * If `account` had not been already granted `role`, emits a {RoleGranted}
     * event.
     *
     * Requirements:
     *
     * - the caller must have ``role``'s admin role.
     */
    function grantRole(bytes32 role, address account) external;

    /**
     * @dev Revokes `role` from `account`.
     *
     * If `account` had been granted `role`, emits a {RoleRevoked} event.
     *
     * Requirements:
     *
     * - the caller must have ``role``'s admin role.
     */
    function revokeRole(bytes32 role, address account) external;

    /**
     * @dev Revokes `role` from the calling account.
     *
     * Roles are often managed via {grantRole} and {revokeRole}: this function's
     * purpose is to provide a mechanism for accounts to lose their privileges
     * if they are compromised (such as when a trusted device is misplaced).
     *
     * If the calling account had been granted `role`, emits a {RoleRevoked}
     * event.
     *
     * Requirements:
     *
     * - the caller must be `callerConfirmation`.
     */
    function renounceRole(bytes32 role, address callerConfirmation) external;
}

// File: @openzeppelin/contracts/utils/introspection/IERC165.sol


// OpenZeppelin Contracts (last updated v5.4.0) (utils/introspection/IERC165.sol)

pragma solidity >=0.4.16;

/**
 * @dev Interface of the ERC-165 standard, as defined in the
 * https://eips.ethereum.org/EIPS/eip-165[ERC].
 *
 * Implementers can declare support of contract interfaces, which can then be
 * queried by others ({ERC165Checker}).
 *
 * For an implementation, see {ERC165}.
 */
interface IERC165 {
    /**
     * @dev Returns true if this contract implements the interface defined by
     * `interfaceId`. See the corresponding
     * https://eips.ethereum.org/EIPS/eip-165#how-interfaces-are-identified[ERC section]
     * to learn more about how these ids are created.
     *
     * This function call must use less than 30 000 gas.
     */
    function supportsInterface(bytes4 interfaceId) external view returns (bool);
}

// File: @openzeppelin/contracts/utils/introspection/ERC165.sol


// OpenZeppelin Contracts (last updated v5.4.0) (utils/introspection/ERC165.sol)

pragma solidity ^0.8.20;


/**
 * @dev Implementation of the {IERC165} interface.
 *
 * Contracts that want to implement ERC-165 should inherit from this contract and override {supportsInterface} to check
 * for the additional interface id that will be supported. For example:
 *
 * ```solidity
 * function supportsInterface(bytes4 interfaceId) public view virtual override returns (bool) {
 *     return interfaceId == type(MyInterface).interfaceId || super.supportsInterface(interfaceId);
 * }
 * ```
 */
abstract contract ERC165 is IERC165 {
    /// @inheritdoc IERC165
    function supportsInterface(bytes4 interfaceId) public view virtual returns (bool) {
        return interfaceId == type(IERC165).interfaceId;
    }
}

// File: @openzeppelin/contracts/access/AccessControl.sol


// OpenZeppelin Contracts (last updated v5.4.0) (access/AccessControl.sol)

pragma solidity ^0.8.20;




/**
 * @dev Contract module that allows children to implement role-based access
 * control mechanisms. This is a lightweight version that doesn't allow enumerating role
 * members except through off-chain means by accessing the contract event logs. Some
 * applications may benefit from on-chain enumerability, for those cases see
 * {AccessControlEnumerable}.
 *
 * Roles are referred to by their `bytes32` identifier. These should be exposed
 * in the external API and be unique. The best way to achieve this is by
 * using `public constant` hash digests:
 *
 * ```solidity
 * bytes32 public constant MY_ROLE = keccak256("MY_ROLE");
 * ```
 *
 * Roles can be used to represent a set of permissions. To restrict access to a
 * function call, use {hasRole}:
 *
 * ```solidity
 * function foo() public {
 *     require(hasRole(MY_ROLE, msg.sender));
 *     ...
 * }
 * ```
 *
 * Roles can be granted and revoked dynamically via the {grantRole} and
 * {revokeRole} functions. Each role has an associated admin role, and only
 * accounts that have a role's admin role can call {grantRole} and {revokeRole}.
 *
 * By default, the admin role for all roles is `DEFAULT_ADMIN_ROLE`, which means
 * that only accounts with this role will be able to grant or revoke other
 * roles. More complex role relationships can be created by using
 * {_setRoleAdmin}.
 *
 * WARNING: The `DEFAULT_ADMIN_ROLE` is also its own admin: it has permission to
 * grant and revoke this role. Extra precautions should be taken to secure
 * accounts that have been granted it. We recommend using {AccessControlDefaultAdminRules}
 * to enforce additional security measures for this role.
 */
abstract contract AccessControl is Context, IAccessControl, ERC165 {
    struct RoleData {
        mapping(address account => bool) hasRole;
        bytes32 adminRole;
    }

    mapping(bytes32 role => RoleData) private _roles;

    bytes32 public constant DEFAULT_ADMIN_ROLE = 0x00;

    /**
     * @dev Modifier that checks that an account has a specific role. Reverts
     * with an {AccessControlUnauthorizedAccount} error including the required role.
     */
    modifier onlyRole(bytes32 role) {
        _checkRole(role);
        _;
    }

    /// @inheritdoc IERC165
    function supportsInterface(bytes4 interfaceId) public view virtual override returns (bool) {
        return interfaceId == type(IAccessControl).interfaceId || super.supportsInterface(interfaceId);
    }

    /**
     * @dev Returns `true` if `account` has been granted `role`.
     */
    function hasRole(bytes32 role, address account) public view virtual returns (bool) {
        return _roles[role].hasRole[account];
    }

    /**
     * @dev Reverts with an {AccessControlUnauthorizedAccount} error if `_msgSender()`
     * is missing `role`. Overriding this function changes the behavior of the {onlyRole} modifier.
     */
    function _checkRole(bytes32 role) internal view virtual {
        _checkRole(role, _msgSender());
    }

    /**
     * @dev Reverts with an {AccessControlUnauthorizedAccount} error if `account`
     * is missing `role`.
     */
    function _checkRole(bytes32 role, address account) internal view virtual {
        if (!hasRole(role, account)) {
            revert AccessControlUnauthorizedAccount(account, role);
        }
    }

    /**
     * @dev Returns the admin role that controls `role`. See {grantRole} and
     * {revokeRole}.
     *
     * To change a role's admin, use {_setRoleAdmin}.
     */
    function getRoleAdmin(bytes32 role) public view virtual returns (bytes32) {
        return _roles[role].adminRole;
    }

    /**
     * @dev Grants `role` to `account`.
     *
     * If `account` had not been already granted `role`, emits a {RoleGranted}
     * event.
     *
     * Requirements:
     *
     * - the caller must have ``role``'s admin role.
     *
     * May emit a {RoleGranted} event.
     */
    function grantRole(bytes32 role, address account) public virtual onlyRole(getRoleAdmin(role)) {
        _grantRole(role, account);
    }

    /**
     * @dev Revokes `role` from `account`.
     *
     * If `account` had been granted `role`, emits a {RoleRevoked} event.
     *
     * Requirements:
     *
     * - the caller must have ``role``'s admin role.
     *
     * May emit a {RoleRevoked} event.
     */
    function revokeRole(bytes32 role, address account) public virtual onlyRole(getRoleAdmin(role)) {
        _revokeRole(role, account);
    }

    /**
     * @dev Revokes `role` from the calling account.
     *
     * Roles are often managed via {grantRole} and {revokeRole}: this function's
     * purpose is to provide a mechanism for accounts to lose their privileges
     * if they are compromised (such as when a trusted device is misplaced).
     *
     * If the calling account had been revoked `role`, emits a {RoleRevoked}
     * event.
     *
     * Requirements:
     *
     * - the caller must be `callerConfirmation`.
     *
     * May emit a {RoleRevoked} event.
     */
    function renounceRole(bytes32 role, address callerConfirmation) public virtual {
        if (callerConfirmation != _msgSender()) {
            revert AccessControlBadConfirmation();
        }

        _revokeRole(role, callerConfirmation);
    }

    /**
     * @dev Sets `adminRole` as ``role``'s admin role.
     *
     * Emits a {RoleAdminChanged} event.
     */
    function _setRoleAdmin(bytes32 role, bytes32 adminRole) internal virtual {
        bytes32 previousAdminRole = getRoleAdmin(role);
        _roles[role].adminRole = adminRole;
        emit RoleAdminChanged(role, previousAdminRole, adminRole);
    }

    /**
     * @dev Attempts to grant `role` to `account` and returns a boolean indicating if `role` was granted.
     *
     * Internal function without access restriction.
     *
     * May emit a {RoleGranted} event.
     */
    function _grantRole(bytes32 role, address account) internal virtual returns (bool) {
        if (!hasRole(role, account)) {
            _roles[role].hasRole[account] = true;
            emit RoleGranted(role, account, _msgSender());
            return true;
        } else {
            return false;
        }
    }

    /**
     * @dev Attempts to revoke `role` from `account` and returns a boolean indicating if `role` was revoked.
     *
     * Internal function without access restriction.
     *
     * May emit a {RoleRevoked} event.
     */
    function _revokeRole(bytes32 role, address account) internal virtual returns (bool) {
        if (hasRole(role, account)) {
            _roles[role].hasRole[account] = false;
            emit RoleRevoked(role, account, _msgSender());
            return true;
        } else {
            return false;
        }
    }
}

// File: remix-backup-at-1h6min-2025-10-11 (1)/.workspaces/SHAGUN-Polygon-Deployment/SHAGUNBase.sol


pragma solidity ^0.8.20;




abstract contract SHAGUNBase is ERC20, Ownable, AccessControl {
    bytes32 public constant ADMIN_ROLE = keccak256("ADMIN_ROLE");

    // Core constants
    uint256 public constant TOTAL_SUPPLY = 40_000_000 * 10**18;
    uint256 public constant MINING_SLOTS = 24;
    uint256 public constant SLOT_DURATION = 15 days;
    uint256 public constant TOTAL_MINING_MONTHS = 12;
    uint256 public constant WITHDRAWAL_FEE_PERCENT = 15;
    uint256 public constant HOLDING_REWARD_PERCENT = 20;
    uint256 public constant HOLDING_DISTRIBUTION_MONTHS = 4;
    uint256 public constant TOKEN_STAKE_RS_WORTH = 10_000;

    // Phase system
    uint256 public currentPhase;
    uint256 public currentPhaseRSValue;
    uint256 public phaseStartTime;
    uint256 public totalRSDistributed;

    // Mining system
    mapping(address => uint256) public userStakedTokens;
    mapping(address => uint256) public userStakeTime;
    mapping(address => uint256) public userMiningSlotsCompleted;
    mapping(address => uint256) public userTotalMiningRewards;
    mapping(address => uint256) public userTotalWithdrawn;

    // Holding rewards (months 13-16)
    mapping(address => uint256) public userMonth12HoldingBalance;
    mapping(address => uint256) public userHoldingRewardsClaimed;
    mapping(address => uint256) public userLastHoldingRewardClaim;

    // Admin addresses
    address public adminAddress;
    address public companyWallet;
    address public liquidityWallet;

    // Control flags
    bool public miningPaused;
    bool public withdrawalsPaused;
    // === Phase override / history (from Fix implementation) ===
    bool public usePhaseOverride;
    uint256 public overridePhase;
    uint256 public overrideRSValue;
    uint256 public overridePhaseStartTime;

    struct PhaseHistory {
        uint256 phaseNumber;
        uint256 rsValue;
        uint256 timestamp;
        address changedBy;
        string reason;
    }
    PhaseHistory[] public phaseHistory;

    // Events
    event MiningSlotClaimed(address indexed user, uint256 slotNumber, uint256 rewardAmount);
    event MiningSlotLapsed(address indexed user, uint256 slotNumber, uint256 lapsedAmount);
    event MiningCompleted(address indexed user, uint256 totalRewards);
    event HoldingRewardDistributed(address indexed user, uint256 month, uint256 rewardAmount);
    event WithdrawalProcessed(address indexed user, uint256 requestedAmount, uint256 feeAmount, uint256 netAmount);
    event PhaseChanged(uint256 indexed oldPhase, uint256 indexed newPhase, uint256 newRSValue);
    event TokensStaked(address indexed user, uint256 amount, uint256 stakeTime);
    event ProductPurchaseApproved(address indexed user, uint256 productId, uint256 quantity, uint256 tokensStaked);
    // FIX: New events
    event PhaseOverrideEnabled(uint256 phase, uint256 rsValue, string reason);
    event PhaseOverrideDisabled(string reason);
    event PhaseManuallyChanged(uint256 oldPhase, uint256 newPhase, uint256 oldRS, uint256 newRS, string reason);
    event ProductPriceSet(uint256 indexed productId, uint256 priceInRS);
    event ProductPurchaseFixed(address indexed user, uint256 productId, uint256 quantity, uint256 correctTokens, uint256 totalRS);

    constructor(
        address _adminAddress,
        address _companyWallet,
        address _liquidityWallet,
        address initialOwner
    ) ERC20("SHAGUN", "SGN") Ownable(initialOwner) {
        require(_adminAddress != address(0), "Invalid admin address");
        require(_companyWallet != address(0), "Invalid company wallet");
        require(_liquidityWallet != address(0), "Invalid liquidity wallet");
        require(initialOwner != address(0), "Invalid owner address");

        _grantRole(DEFAULT_ADMIN_ROLE, initialOwner);
        _grantRole(ADMIN_ROLE, initialOwner);

        adminAddress = _adminAddress;
        companyWallet = _companyWallet;
        liquidityWallet = _liquidityWallet;

        _mint(_adminAddress, TOTAL_SUPPLY);

        currentPhase = 1;
        currentPhaseRSValue = 4e18;
        phaseStartTime = block.timestamp;
        miningPaused = false;
        withdrawalsPaused = false;

        // Initialize a genesis phase history entry
        phaseHistory.push(PhaseHistory({
            phaseNumber: currentPhase,
            rsValue: currentPhaseRSValue,
            timestamp: block.timestamp,
            changedBy: address(0),
            reason: "Genesis state"
        }));
    }

    // Admin functions
    function setMiningPaused(bool _paused) external onlyOwner {
        miningPaused = _paused;
    }

    function setWithdrawalsPaused(bool _paused) external onlyOwner {
        withdrawalsPaused = _paused;
    }

    function updateAdminAddress(address _adminAddress) external onlyOwner {
        require(_adminAddress != address(0), "Invalid admin address");
        adminAddress = _adminAddress;
    }

    function updateCompanyWallet(address _companyWallet) external onlyOwner {
        require(_companyWallet != address(0), "Invalid company wallet");
        companyWallet = _companyWallet;
    }

    // manualChangePhase removed. Use `enablePhaseOverride` instead.

    function recoverTokens(address token, address to, uint256 amount) external onlyOwner {
        require(to != address(0), "Cannot recover to zero address");
        if (token == address(0)) {
            payable(to).transfer(amount);
        } else {
            IERC20(token).transfer(to, amount);
        }
    }

    // === Admin functions for phase override/history ===
    function enablePhaseOverride(
        uint256 newPhase,
        uint256 newRSValue,
        string calldata reason
    ) external onlyOwner {
        require(newRSValue > 0, "RS value must be > 0");
        uint256 oldPhase = _getCurrentPhase();
        uint256 oldRS = _getCurrentRSValue();

        usePhaseOverride = true;
        overridePhase = newPhase;
        overrideRSValue = newRSValue;
        overridePhaseStartTime = block.timestamp;

        // Apply override to main phase variables so storage reflects the manual change
        // This ensures `currentPhase` and `currentPhaseRSValue` show the effective values
        currentPhase = newPhase;
        currentPhaseRSValue = newRSValue;
        phaseStartTime = block.timestamp;

        phaseHistory.push(PhaseHistory({
            phaseNumber: newPhase,
            rsValue: newRSValue,
            timestamp: block.timestamp,
            changedBy: msg.sender,
            reason: reason
        }));

        emit PhaseOverrideEnabled(newPhase, newRSValue, reason);
        emit PhaseManuallyChanged(oldPhase, newPhase, oldRS, newRSValue, reason);
    }

    function disablePhaseOverride(string calldata reason) external onlyOwner {
        uint256 oldPhase = _getCurrentPhase();
        uint256 oldRS = _getCurrentRSValue();

        usePhaseOverride = false;

        phaseHistory.push(PhaseHistory({
            phaseNumber: currentPhase,
            rsValue: currentPhaseRSValue,
            timestamp: block.timestamp,
            changedBy: msg.sender,
            reason: reason
        }));

        emit PhaseOverrideDisabled(reason);
        emit PhaseManuallyChanged(oldPhase, currentPhase, oldRS, currentPhaseRSValue, reason);
    }

    function getPhaseHistory(uint256 index) external view returns (PhaseHistory memory) {
        require(index < phaseHistory.length, "Invalid history index");
        return phaseHistory[index];
    }

    function phaseHistoryLength() external view returns (uint256) {
        return phaseHistory.length;
    }

    // Internal functions
    function _getCurrentPhase() internal view returns (uint256) {
        return usePhaseOverride ? overridePhase : currentPhase;
    }

    function _getCurrentRSValue() internal view returns (uint256) {
        return usePhaseOverride ? overrideRSValue : currentPhaseRSValue;
    }

    function _calculateTokensForRS(uint256 rsAmount) internal view returns (uint256) {
        uint256 rsValue = _getCurrentRSValue();
        require(rsValue > 0, "RS value is zero");
        return (rsAmount * 1e18 * 10) / (rsValue / 1e17);
    }

    function _calculateRSAgainstTokens(uint256 tokenAmount) internal view returns (uint256) {
        uint256 rsValue = _getCurrentRSValue();
        require(rsValue > 0, "RS value is zero");
        return tokenAmount * rsValue / (10**18);
    }

    function approve(address spender, uint256 amount) public override returns (bool) {
        require(spender != address(0), "ERC20: approve to zero address");
        return super.approve(spender, amount);
    }

    receive() external payable {}
}

// File: remix-backup-at-1h6min-2025-10-11 (1)/.workspaces/SHAGUN-Polygon-Deployment/SHAGUNPurchaseAndPhases.sol


pragma solidity ^0.8.20;


abstract contract SHAGUNPurchaseAndPhases is SHAGUNBase {
    // Initialize product prices (whole RS units)
    constructor(
        address _adminAddress,
        address _companyWallet,
        address _liquidityWallet,
        address initialOwner
    ) SHAGUNBase(_adminAddress, _companyWallet, _liquidityWallet, initialOwner) {
        // Set default product prices in whole RS units (e.g., 10000)
        productPriceInRS[1] = 10000;
        productPriceInRS[2] = 10000;
        emit ProductPriceSet(1, productPriceInRS[1]);
        emit ProductPriceSet(2, productPriceInRS[2]);
    }

    // Product price mapping (in whole RS units, e.g., 10000)
    mapping(uint256 => uint256) public productPriceInRS;

    // STAKING FUNCTIONS
    function stakeTokensForUser(address user) external onlyOwner {
        require(user != address(0), "Invalid user address");
        uint256 tokensToStake = _calculateTokensForRS(TOKEN_STAKE_RS_WORTH);
        require(balanceOf(adminAddress) >= tokensToStake, "Insufficient tokens in admin wallet");

        _transfer(adminAddress, address(this), tokensToStake);

        if (userStakedTokens[user] == 0) {
            userStakeTime[user] = block.timestamp;
        }
        userStakedTokens[user] = userStakedTokens[user] + tokensToStake;
        totalRSDistributed = totalRSDistributed + TOKEN_STAKE_RS_WORTH;
        // store this now, frontend will check 12 months
        userMonth12HoldingBalance[msg.sender] = userStakedTokens[msg.sender];

        emit TokensStaked(user, tokensToStake, block.timestamp);
    }

    function stakeTokensForMultipleUsers(address[] calldata users) external onlyOwner {
        for (uint256 i = 0; i < users.length; i++) {
            require(users[i] != address(0), "Invalid user address");
            uint256 tokensToStake = _calculateTokensForRS(TOKEN_STAKE_RS_WORTH);
            require(balanceOf(adminAddress) >= tokensToStake, "Insufficient tokens in admin wallet");

            _transfer(adminAddress, address(this), tokensToStake);

            if (userStakedTokens[users[i]] == 0) {
                userStakeTime[users[i]] = block.timestamp;
            }
            userStakedTokens[users[i]] = userStakedTokens[users[i]] + tokensToStake;
            totalRSDistributed = totalRSDistributed + TOKEN_STAKE_RS_WORTH;

            emit TokensStaked(users[i], tokensToStake, block.timestamp);
        }
    }

    // PRODUCT PURCHASE APPROVAL
    function approveProductPurchase(address user, uint256 productId, uint256 quantity) external onlyOwner {
        require(user != address(0), "Invalid user address");
        require(productId == 1 || productId == 2, "Invalid product ID - only 1 or 2 allowed");
        require(quantity > 0, "Quantity must be greater than 0");
        require(quantity <= 100, "Maximum 100 packages per purchase");
        uint256 tokensToStake;
        if (productPriceInRS[productId] > 0) {
            uint256 pricePerUnit = productPriceInRS[productId];
            uint256 totalRS = pricePerUnit * quantity; // whole RS units
            tokensToStake = _calculateTokensForRS(totalRS);
            totalRSDistributed = totalRSDistributed + totalRS;
            emit ProductPurchaseFixed(user, productId, quantity, tokensToStake, totalRS);
        } else {
            tokensToStake = _calculateTokensForRS(TOKEN_STAKE_RS_WORTH * quantity);
            totalRSDistributed = totalRSDistributed + (TOKEN_STAKE_RS_WORTH * quantity);
            emit ProductPurchaseApproved(user, productId, quantity, tokensToStake);
        }

        require(balanceOf(adminAddress) >= tokensToStake, "Insufficient tokens in admin wallet");
        _transfer(adminAddress, address(this), tokensToStake);
        if (userStakedTokens[user] == 0) {
            userStakeTime[user] = block.timestamp;
        }
        userStakedTokens[user] = userStakedTokens[user] + tokensToStake;
        emit TokensStaked(user, tokensToStake, block.timestamp);
    }

    function approveMultipleProductPurchases(
        address[] calldata users,
        uint256[] calldata productIds,
        uint256[] calldata quantities
    ) external onlyOwner {
        require(users.length == productIds.length && productIds.length == quantities.length, "Arrays must match");

        for (uint256 i = 0; i < users.length; i++) {
            require(users[i] != address(0), "Invalid user address");
            require(productIds[i] == 1 || productIds[i] == 2, "Invalid product ID - only 1 or 2 allowed");
            require(quantities[i] > 0, "Quantity must be greater than 0");
            require(quantities[i] <= 100, "Maximum 100 packages per purchase");

            uint256 tokensToStake;
            if (productPriceInRS[productIds[i]] > 0) {
                uint256 pricePerUnit = productPriceInRS[productIds[i]];
                uint256 totalRS = pricePerUnit * quantities[i];
                tokensToStake = _calculateTokensForRS(totalRS);
                totalRSDistributed = totalRSDistributed + totalRS;
                emit ProductPurchaseFixed(users[i], productIds[i], quantities[i], tokensToStake, totalRS);
            } else {
                tokensToStake = _calculateTokensForRS(TOKEN_STAKE_RS_WORTH * quantities[i]);
                totalRSDistributed = totalRSDistributed + (TOKEN_STAKE_RS_WORTH * quantities[i]);
                emit ProductPurchaseApproved(users[i], productIds[i], quantities[i], tokensToStake);
            }
            require(balanceOf(adminAddress) >= tokensToStake, "Insufficient tokens in admin wallet");

            _transfer(adminAddress, address(this), tokensToStake);

            if (userStakedTokens[users[i]] == 0) {
                userStakeTime[users[i]] = block.timestamp;
            }
            userStakedTokens[users[i]] = userStakedTokens[users[i]] + tokensToStake;
            emit TokensStaked(users[i], tokensToStake, block.timestamp);
        }
    }

    function setProductPrice(uint256 productId, uint256 priceInRS) external onlyOwner {
        require(productId == 1 || productId == 2, "Invalid product ID");
        require(priceInRS > 0, "Price must be > 0");
        productPriceInRS[productId] = priceInRS;
        emit ProductPriceSet(productId, priceInRS);
    }

    // PHASE MANAGEMENT
    function checkAndUpdatePhase() internal {
        bool shouldChange = false;

        if (block.timestamp >= phaseStartTime + 30 days) {
            shouldChange = true;
        }

        if (totalRSDistributed >= 3_000_000) {
            shouldChange = true;
        }

        if (shouldChange) {
            uint256 oldPhase = currentPhase;
            currentPhase = currentPhase + 1;
            currentPhaseRSValue = currentPhaseRSValue * 120 / 100;
            phaseStartTime = block.timestamp;
            totalRSDistributed = 0;

            emit PhaseChanged(oldPhase, currentPhase, currentPhaseRSValue);
        }
    }

    function getCurrentPhase() external view returns (
        uint256 phaseNumber,
        uint256 rsValue,
        uint256 startTime,
        uint256 totalDistributed
    ) {
        if (usePhaseOverride) {
            return (overridePhase, overrideRSValue, overridePhaseStartTime, totalRSDistributed);
        }
        return (currentPhase, currentPhaseRSValue, phaseStartTime, totalRSDistributed);
    }

    function calculateTokensForRS(uint256 rsAmount) external view returns (uint256 tokens) {
        return _calculateTokensForRS(rsAmount);
    }

    function calculateRSForTokens(uint256 tokenAmount) external view returns (uint256 rsAmount) {
        return _calculateRSAgainstTokens(tokenAmount);
    }
}

// File: remix-backup-at-1h6min-2025-10-11 (1)/.workspaces/SHAGUN-Polygon-Deployment/SHAGUNMining.sol


pragma solidity ^0.8.20;


abstract contract SHAGUNMining is SHAGUNPurchaseAndPhases {

    // Additional mining events
    event MiningClaimed(address indexed user, uint256 slotNumber, uint256 rewardAmount);
    event HoldingRewardCalculated(address indexed user, uint256 month12Balance, uint256 totalHoldingRewards);
    event ForceMiningWithdraw(address indexed user, uint256 totalAmount, uint256 feeAmount, uint256 netAmount);

    constructor(
        address _adminAddress,
        address _companyWallet,
        address _liquidityWallet,
        address initialOwner
    ) SHAGUNPurchaseAndPhases(_adminAddress, _companyWallet, _liquidityWallet, initialOwner) {}

    // MINING FUNCTIONS
    function claimMiningRewards() external {
        require(!miningPaused, "Mining is paused");
        require(userStakedTokens[msg.sender] > 0, "No tokens staked");

        uint256 currentTime = block.timestamp;
        uint256 stakeTime = userStakeTime[msg.sender];
        uint256 monthsElapsed = (currentTime - stakeTime) / (30 days);

        require(monthsElapsed <= TOTAL_MINING_MONTHS, "Mining period completed");
        require(userMiningSlotsCompleted[msg.sender] < MINING_SLOTS, "All mining slots completed");

        uint256 currentSlot = ((currentTime - stakeTime) / SLOT_DURATION) + 1;
        require(currentSlot > userMiningSlotsCompleted[msg.sender], "Current slot not yet available");

        if (currentSlot > MINING_SLOTS) {
            currentSlot = MINING_SLOTS;
        }

        uint256 slotsToClaim = currentSlot - userMiningSlotsCompleted[msg.sender];
        require(slotsToClaim > 0, "No slots available to claim");

        // Auto-process lapsed slots (exclude current claimable slot)
        if (currentSlot > userMiningSlotsCompleted[msg.sender] + 1) {
            uint256 lapsedSlots = currentSlot - userMiningSlotsCompleted[msg.sender] - 1;
            if (lapsedSlots > MINING_SLOTS - userMiningSlotsCompleted[msg.sender] - 1) {
                lapsedSlots = MINING_SLOTS - userMiningSlotsCompleted[msg.sender] - 1;
            }

            if (lapsedSlots > 0) {
                uint256 baseSlotRewardLapsed = userStakedTokens[msg.sender] / MINING_SLOTS;
                uint256 totalLapsedReward = baseSlotRewardLapsed * lapsedSlots;

                uint256 companyRewardLapsed = totalLapsedReward * 70 / 100;
                uint256 burnRewardLapsed = totalLapsedReward * 15 / 100;
                uint256 liquidityRewardLapsed = totalLapsedReward * 15 / 100;

                if (companyRewardLapsed > 0) {
                    _transfer(address(this), companyWallet, companyRewardLapsed);
                }
                if (burnRewardLapsed > 0) {
                    _burn(address(this), burnRewardLapsed);
                }
                if (liquidityRewardLapsed > 0) {
                    _transfer(address(this), liquidityWallet, liquidityRewardLapsed);
                }

                userMiningSlotsCompleted[msg.sender] = userMiningSlotsCompleted[msg.sender] + lapsedSlots;
                emit MiningSlotLapsed(msg.sender, userMiningSlotsCompleted[msg.sender], totalLapsedReward);
            }
        }

        // Process current slot
        uint256 currentCompleted = userMiningSlotsCompleted[msg.sender];
        uint256 nextSlotToClaim = currentCompleted + 1;

        uint256 slotStartTime = stakeTime + (nextSlotToClaim - 1) * SLOT_DURATION;
        uint256 slotEndTime = slotStartTime + SLOT_DURATION;
        require(currentTime <= slotEndTime, "Current slot claim window has expired.");

        uint256 baseSlotReward = userStakedTokens[msg.sender] / MINING_SLOTS;
        uint256 userReward = baseSlotReward * 20 / 100;
        uint256 companyReward = baseSlotReward * 50 / 100;
        uint256 burnReward = baseSlotReward * 15 / 100;
        uint256 liquidityReward = baseSlotReward * 15 / 100;

        if (userReward > 0) {
            _transfer(address(this), msg.sender, userReward);
        }
        if (companyReward > 0) {
            _transfer(address(this), companyWallet, companyReward);
        }
        if (burnReward > 0) {
            _burn(address(this), burnReward);
        }
        if (liquidityReward > 0) {
            _transfer(address(this), liquidityWallet, liquidityReward);
        }

        userMiningSlotsCompleted[msg.sender] = userMiningSlotsCompleted[msg.sender] + 1;
        userTotalMiningRewards[msg.sender] = userTotalMiningRewards[msg.sender] + userReward;

        // if (monthsElapsed >= 12 && userMonth12HoldingBalance[msg.sender] == 0) {
            _calculateMonth12HoldingBalance(msg.sender);
        // }

        emit MiningClaimed(msg.sender, nextSlotToClaim, userReward);

        if (userMiningSlotsCompleted[msg.sender] >= MINING_SLOTS) {
            emit MiningCompleted(msg.sender, userTotalMiningRewards[msg.sender]);
        }
    }

    function forceMiningWithdraw() external {
        require(!withdrawalsPaused, "Withdrawals are paused");

        uint256 stakeTime = userStakeTime[msg.sender];
        uint256 monthsElapsed = (block.timestamp - stakeTime) / (30 days);

        require(monthsElapsed >= 16, "Can only force withdraw after 16 months");

        uint256 totalEarnedRewards = userTotalMiningRewards[msg.sender] - userTotalWithdrawn[msg.sender];
        require(totalEarnedRewards > 0, "No earned rewards to withdraw");

        uint256 feeAmount = totalEarnedRewards * 15 / 100;
        uint256 burnAmount = totalEarnedRewards * 15 / 100;
        uint256 companyAmount = totalEarnedRewards * 70 / 100;

        if (feeAmount > 0) {
            _transfer(address(this), companyWallet, feeAmount);
        }
        if (companyAmount > 0) {
            _transfer(address(this), companyWallet, companyAmount);
        }
        if (burnAmount > 0) {
            _burn(address(this), burnAmount);
        }

        userTotalWithdrawn[msg.sender] = userTotalWithdrawn[msg.sender] + totalEarnedRewards;

        emit ForceMiningWithdraw(msg.sender, totalEarnedRewards, feeAmount + companyAmount, burnAmount);
        emit WithdrawalProcessed(msg.sender, totalEarnedRewards, feeAmount + companyAmount, burnAmount);
    }

    // Virtual function to be implemented in derived contracts
    function _calculateMonth12HoldingBalance(address user) internal virtual;
}
// File: remix-backup-at-1h6min-2025-10-11 (1)/.workspaces/SHAGUN-Polygon-Deployment/SHAGUNWithdrawals.sol


pragma solidity ^0.8.20;


abstract contract SHAGUNWithdrawals is SHAGUNMining {

    constructor(
        address _adminAddress,
        address _companyWallet,
        address _liquidityWallet,
        address initialOwner
    ) SHAGUNMining(_adminAddress, _companyWallet, _liquidityWallet, initialOwner) {}

    // HOLDING REWARDS FUNCTIONS
    function _calculateMonth12HoldingBalance(address user) internal override {
        uint256 month12Balance = userTotalMiningRewards[user] - userTotalWithdrawn[user];
        userMonth12HoldingBalance[user] = month12Balance;

        uint256 totalHoldingRewards = month12Balance * HOLDING_REWARD_PERCENT / 100;
        userHoldingRewardsClaimed[user] = 0;

        emit HoldingRewardCalculated(user, month12Balance, totalHoldingRewards);
    }


    /// @notice Claim 5% holding reward based on your ACTUAL token balance in wallet
/// @dev Can be claimed once every 30 days – no 12-month wait, no snapshots, no manual math
function claimHoldingRewards() external {
    require(!withdrawalsPaused, "Withdrawals paused");

    // Use REAL on-chain balance – this is what user actually holds in their wallet
    uint256 userRealBalance = balanceOf(msg.sender);
    require(userRealBalance > 0, "No tokens in wallet");

    // One claim per 30 days
    require(
        block.timestamp >= userLastHoldingRewardClaim[msg.sender] + 30 days,
        "Must wait 30 days between claims"
    );

    // 5% reward of user's actual current balance
    uint256 reward = userRealBalance * 5 / 100;

    // Transfer reward directly from contract to user
    _transfer(adminAddress, msg.sender, reward);

    // Update last claim time
    userLastHoldingRewardClaim[msg.sender] = block.timestamp;

    // Optional counter
    userHoldingRewardsClaimed[msg.sender] += 1;

    emit HoldingRewardDistributed(msg.sender, userHoldingRewardsClaimed[msg.sender], reward);
}


    // WITHDRAWAL FUNCTIONS
    function withdrawTokens(uint256 amount) external {
        require(!withdrawalsPaused, "Withdrawals are paused");
        require(amount > 0, "Amount must be greater than 0");

        uint256 availableRewards = userTotalMiningRewards[msg.sender] - userTotalWithdrawn[msg.sender];
        require(availableRewards >= amount, "Insufficient earned rewards");

        uint256 feeAmount = amount * 15 / 100;
        uint256 burnAmount = amount * 15 / 100;
        uint256 companyAmount = amount * 70 / 100;

        if (feeAmount > 0) {
            _transfer(address(this), companyWallet, feeAmount);
        }
        if (companyAmount > 0) {
            _transfer(address(this), companyWallet, companyAmount);
        }
        if (burnAmount > 0) {
            _burn(address(this), burnAmount);
        }

        userTotalWithdrawn[msg.sender] = userTotalWithdrawn[msg.sender] + amount;
    }
  }

// File: remix-backup-at-1h6min-2025-10-11 (1)/.workspaces/SHAGUN-Polygon-Deployment/SHAGUNViews.sol


  pragma solidity ^0.8.20;


  abstract contract SHAGUNViews is SHAGUNWithdrawals {

      constructor(
          address _adminAddress,
          address _companyWallet,
          address _liquidityWallet,
          address initialOwner
      ) SHAGUNWithdrawals(_adminAddress, _companyWallet, _liquidityWallet, initialOwner) {}

      // VIEW FUNCTIONS
      function getUserMiningInfo(address user) external view returns (
          uint256 stakedTokens,
          uint256 stakeTime,
          uint256 slotsCompleted,
          uint256 slotsRemaining,
          uint256 currentSlot,
          uint256 totalRewards,
          uint256 nextClaimTime,
          bool miningCompleted
      ) {
          stakedTokens = userStakedTokens[user];
          stakeTime = userStakeTime[user];
          slotsCompleted = userMiningSlotsCompleted[user];
          currentSlot = (block.timestamp - stakeTime) / SLOT_DURATION;
          totalRewards = userTotalMiningRewards[user];
          miningCompleted = slotsCompleted >= MINING_SLOTS;
          slotsRemaining = MINING_SLOTS - slotsCompleted;
          nextClaimTime = stakeTime + (currentSlot + 1) * SLOT_DURATION;
      }

      function getUserHoldingInfo(address user) external view returns (
          uint256 month12Balance,
          uint256 totalHoldingRewards,
          uint256 rewardsClaimed,
          uint256 currentMonth,
          bool canClaimThisMonth,
          uint256 claimableAmount
      ) {
          month12Balance = userMonth12HoldingBalance[user];
          rewardsClaimed = userHoldingRewardsClaimed[user];
          uint256 stakeTime_ = userStakeTime[user];
          currentMonth = (block.timestamp - stakeTime_) / (30 days);
          totalHoldingRewards = month12Balance * HOLDING_REWARD_PERCENT / 100;
          bool inClaimPeriod = currentMonth >= 13 && currentMonth <= 16;
          uint256 monthIndex = currentMonth - 12;
          canClaimThisMonth = inClaimPeriod && monthIndex > rewardsClaimed;
          claimableAmount = canClaimThisMonth ? totalHoldingRewards / HOLDING_DISTRIBUTION_MONTHS : 0;
      }

      function getUserInfo(address user) external view virtual returns (
          uint256 stakedTokens,
          uint256 totalMiningRewards,
          uint256 totalWithdrawn,
          uint256 month12HoldingBalance,
          uint256 miningSlotsCompleted,
          uint256 holdingRewardsClaimed,
          uint256 currentPhase,
          uint256 stakeTime
      ) {
          uint256 _currentPhase = currentPhase;
          return (
              userStakedTokens[user],
              userTotalMiningRewards[user],
              userTotalWithdrawn[user],
              userMonth12HoldingBalance[user],
              userMiningSlotsCompleted[user],
              userHoldingRewardsClaimed[user],
              _currentPhase,
              userStakeTime[user]
          );
      }

      function getContractStats() external view returns (
          uint256 _totalSupply,
          uint256 currentPhase,
          uint256 phaseRSValue,
          uint256 totalRSDistributed,
          bool miningPaused,
          bool withdrawalsPaused
      ) {
          uint256 effectivePhase = _getCurrentPhase();
          uint256 effectiveRS = _getCurrentRSValue();
          return (
              totalSupply(),
              effectivePhase,
              effectiveRS,
              totalRSDistributed,
              miningPaused,
              withdrawalsPaused
          );
      }

      function calculateWithdrawal(uint256 rewardAmount) external view returns (
          uint256 rsValue,
          uint256 feeAmount,
          uint256 netRSValue
      ) {
          rsValue = _calculateRSAgainstTokens(rewardAmount);
          feeAmount = rsValue * 85 / 100;
          netRSValue = rsValue * 15 / 100;
      }
  }
// File: remix-backup-at-1h6min-2025-10-11 (1)/.workspaces/SHAGUN-Polygon-Deployment/SHAGUNViewsExtra.sol


pragma solidity ^0.8.20;


abstract contract SHAGUNViewsExtra is SHAGUNViews {

    constructor(
        address _adminAddress,
        address _companyWallet,
        address _liquidityWallet,
        address initialOwner
    ) SHAGUNViews(_adminAddress, _companyWallet, _liquidityWallet, initialOwner) {}

    // SPLIT VIEW FUNCTIONS - To avoid stack too deep errors

    // Get staking info separately
    function getUserStakingInfo(address user) external view returns (
        uint256 stakedTokens,
        uint256 stakeTime,
        uint256 miningSlotsCompleted
    ) {
        return (
            userStakedTokens[user],
            userStakeTime[user],
            userMiningSlotsCompleted[user]
        );
    }

    // Get rewards info separately
    function getUserRewardsInfo(address user) external view returns (
        uint256 totalMiningRewards,
        uint256 totalWithdrawn,
        uint256 month12HoldingBalance
    ) {
        return (
            userTotalMiningRewards[user],
            userTotalWithdrawn[user],
            userMonth12HoldingBalance[user]
        );
    }

    // Get holding info separately
    function getUserHoldingClaimsInfo(address user) external view returns (
        uint256 holdingRewardsClaimed,
        uint256 currentPhase
    ) {
        return (
            userHoldingRewardsClaimed[user],
            currentPhase
        );
    }

    // Combined but smaller chunks
    function getUserInfoPart1(address user) external view returns (
        uint256 stakedTokens,
        uint256 totalMiningRewards,
        uint256 totalWithdrawn,
        uint256 stakeTime
    ) {
        return (
            userStakedTokens[user],
            userTotalMiningRewards[user],
            userTotalWithdrawn[user],
            userStakeTime[user]
        );
    }

    function getUserInfoPart2(address user) external view returns (
        uint256 month12HoldingBalance,
        uint256 miningSlotsCompleted,
        uint256 holdingRewardsClaimed,
        uint256 currentPhase
    ) {
        return (
            userMonth12HoldingBalance[user],
            userMiningSlotsCompleted[user],
            userHoldingRewardsClaimed[user],
            currentPhase
        );
    }
}
// File: remix-backup-at-1h6min-2025-10-11 (1)/.workspaces/SHAGUN-Polygon-Deployment/SHAGUNViewsSafe.sol


pragma solidity ^0.8.20;


abstract contract SHAGUNViewsSafe is SHAGUNViewsExtra {

    constructor(
        address _adminAddress,
        address _companyWallet,
        address _liquidityWallet,
        address initialOwner
    ) SHAGUNViewsExtra(_adminAddress, _companyWallet, _liquidityWallet, initialOwner) {}

    // ============================================================================
    // OVERRIDE PROBLEMATIC FUNCTION WITH SAFER IMPLEMENTATION
    // ============================================================================

    /**
     * @notice Safer version of getUserInfo - splits return values to avoid stack overflow
     * @dev Uses intermediate variables to reduce stack pressure during execution
     * @param user Address of the user to query
     * @return stakedTokens Total tokens staked by the user
     * @return totalMiningRewards Total mining rewards earned
     * @return totalWithdrawn Total amount withdrawn by the user
     * @return month12HoldingBalance Month 12 holding balance
     * @return miningSlotsCompleted Number of mining slots completed
     * @return holdingRewardsClaimed Holding rewards that have been claimed
     * @return currentPhase_ Current phase number
     * @return stakeTime Timestamp when user first staked
     */
    function getUserInfo(address user) external view override returns (
        uint256 stakedTokens,
        uint256 totalMiningRewards,
        uint256 totalWithdrawn,
        uint256 month12HoldingBalance,
        uint256 miningSlotsCompleted,
        uint256 holdingRewardsClaimed,
        uint256 currentPhase_,
        uint256 stakeTime
    ) {
        stakedTokens = userStakedTokens[user];
        totalMiningRewards = userTotalMiningRewards[user];
        totalWithdrawn = userTotalWithdrawn[user];
        month12HoldingBalance = userMonth12HoldingBalance[user];
        miningSlotsCompleted = userMiningSlotsCompleted[user];
        holdingRewardsClaimed = userHoldingRewardsClaimed[user];
        currentPhase_ = currentPhase;
        stakeTime = userStakeTime[user];
    }

    // ============================================================================
    // ADDITIONAL SAFE VIEW FUNCTIONS - GROUPED BY CATEGORY
    // ============================================================================

    /**
     * @notice Get comprehensive balance summary for a user
     * @dev Returns 4 key balance metrics in a single call
     * @param user Address of the user to query
     * @return stakedTokens Total staked amount
     * @return availableRewards Rewards available for withdrawal (total - withdrawn)
     * @return totalWithdrawn Amount already withdrawn
     * @return netPosition Total value (staked + available rewards)
     */
    function getUserBalanceSummary(address user) external view returns (
        uint256 stakedTokens,
        uint256 availableRewards,
        uint256 totalWithdrawn,
        uint256 netPosition
    ) {
        stakedTokens = userStakedTokens[user];
        uint256 totalMiningRewards_ = userTotalMiningRewards[user];
        totalWithdrawn = userTotalWithdrawn[user];
        availableRewards = totalMiningRewards_ > totalWithdrawn ? totalMiningRewards_ - totalWithdrawn : 0;
        netPosition = stakedTokens + availableRewards;
    }

    /**
     * @notice Get mining progress statistics for a user
     * @dev Shows slots completed, total slots, and completion percentage
     * @param user Address of the user to query
     * @return slotsCompleted Number of mining slots completed
     * @return totalSlots Total number of mining slots available
     * @return progressPercentage Completion percentage (0-100)
     */
    function getUserMiningProgress(address user) external view returns (
        uint256 slotsCompleted,
        uint256 totalSlots,
        uint256 progressPercentage
    ) {
        uint256 _slotsCompleted = userMiningSlotsCompleted[user];
        uint256 _totalSlots = MINING_SLOTS;
       
        // Calculate percentage (avoid division by zero)
        uint256 _progressPercentage = _totalSlots > 0
            ? (_slotsCompleted * 100) / _totalSlots
            : 0;

        return (
            _slotsCompleted,
            _totalSlots,
            _progressPercentage
        );
    }

    /**
     * @notice Get phase-related information for a user
     * @dev Returns current phase and user's stake timestamp
     * @param user Address of the user to query
     * @return currentPhase The current phase number
     * @return stakeTime Timestamp when user first staked tokens
     */
    function getUserPhaseInfo(address user) external view returns (
        uint256 currentPhase,
        uint256 stakeTime
    ) {
        return (
            _getCurrentPhase(),
            userStakeTime[user]
        );
    }

    // ============================================================================
    // ADDITIONAL UTILITY VIEW FUNCTIONS (SAFE HELPERS)
    // ============================================================================

    /**
     * @notice Check if a user has any staked tokens
     * @param user Address to check
     * @return hasStake True if user has staked tokens
     */
    function hasUserStaked(address user) external view returns (bool hasStake) {
        return userStakedTokens[user] > 0;
    }

    /**
     * @notice Get time elapsed since user's first stake
     * @param user Address of the user
     * @return timeElapsed Seconds elapsed since stake time (0 if not staked)
     */
    function getStakeDuration(address user) external view returns (uint256 timeElapsed) {
        uint256 _stakeTime = userStakeTime[user];
       
        if (_stakeTime == 0) {
            return 0;
        }
       
        return block.timestamp > _stakeTime
            ? block.timestamp - _stakeTime
            : 0;
    }

    /**
     * @notice Get user's total rewards (mining + holding)
     * @param user Address of the user
     * @return totalRewards Combined mining and holding rewards
     */
    function getUserTotalRewards(address user) external view returns (uint256 totalRewards) {
        uint256 _miningRewards = userTotalMiningRewards[user];
        uint256 _holdingBalance = userMonth12HoldingBalance[user];
       
        return _miningRewards + _holdingBalance;
    }

    /**
     * @notice Get user's claimable rewards (not yet withdrawn)
     * @param user Address of the user
     * @return claimableRewards Amount available to claim
     */
    function getUserClaimableRewards(address user) external view returns (uint256 claimableRewards) {
        uint256 _totalMiningRewards = userTotalMiningRewards[user];
        uint256 _totalWithdrawn = userTotalWithdrawn[user];
       
        return _totalMiningRewards > _totalWithdrawn
            ? _totalMiningRewards - _totalWithdrawn
            : 0;
    }
}
// File: remix-backup-at-1h6min-2025-10-11 (1)/.workspaces/SHAGUN-Polygon-Deployment/SHAGUNViewsMinimal.sol


pragma solidity ^0.8.20;


abstract contract SHAGUNViewsMinimal is SHAGUNViewsSafe {

    constructor(
        address _adminAddress,
        address _companyWallet,
        address _liquidityWallet,
        address initialOwner
    ) SHAGUNViewsSafe(_adminAddress, _companyWallet, _liquidityWallet, initialOwner) {}

    // ULTRA-SAFE GETUSERINFO - Split into multiple tiny functions

    // Get only staking data (2 values)
    function getUserStakingData(address user) public view returns (
        uint256 stakedTokens,
        uint256 stakeTime
    ) {
        return (
            userStakedTokens[user],
            userStakeTime[user]
        );
    }

    // Get only mining data (2 values)
    function getUserMiningData(address user) public view returns (
        uint256 miningSlotsCompleted,
        uint256 totalRewards
    ) {
        return (
            userMiningSlotsCompleted[user],
            userTotalMiningRewards[user]
        );
    }

    // Get only withdrawal data (1 value)
    function getUserWithdrawalData(address user) public view returns (
        uint256 totalWithdrawn
    ) {
        return userTotalWithdrawn[user];
    }

    // Get only holding data (2 values)
    function getUserHoldingData(address user) public view returns (
        uint256 month12HoldingBalance,
        uint256 holdingRewardsClaimed
    ) {
        return (
            userMonth12HoldingBalance[user],
            userHoldingRewardsClaimed[user]
        );
    }

    // ULTRA-MINIMAL getUserInfo - uses named returns to avoid stack issues
    function getUserInfoMinimal(address user) external view returns (
        uint256 stakedTokens,
        uint256 stakeTime,
        uint256 miningSlotsCompleted,
        uint256 totalRewards,
        uint256 totalWithdrawn,
        uint256 month12HoldingBalance,
        uint256 holdingRewardsClaimed,
        uint256 currentPhase_
    ) {
        // Assign directly to named return variables (no local variables = no stack pressure)
        stakedTokens = userStakedTokens[user];
        stakeTime = userStakeTime[user];
        miningSlotsCompleted = userMiningSlotsCompleted[user];
        totalRewards = userTotalMiningRewards[user];
        totalWithdrawn = userTotalWithdrawn[user];
        month12HoldingBalance = userMonth12HoldingBalance[user];
        holdingRewardsClaimed = userHoldingRewardsClaimed[user];
        currentPhase_ = currentPhase;
    }

    function getUserInfoPartA(address user) external view returns (
        uint256 stakedTokens,
        uint256 stakeTime,
        uint256 miningSlotsCompleted,
        uint256 totalRewards
    ) {
        return (
            userStakedTokens[user],
            userStakeTime[user],
            userMiningSlotsCompleted[user],
            userTotalMiningRewards[user]
        );
    }

    function getUserInfoPartB(address user) external view returns (
        uint256 totalWithdrawn,
        uint256 month12HoldingBalance,
        uint256 holdingRewardsClaimed,
        uint256 currentPhase
    ) {
        return (
            userTotalWithdrawn[user],
            userMonth12HoldingBalance[user],
            userHoldingRewardsClaimed[user],
            currentPhase
        );
    }

    // For frontend: combine the two parts
    function getUserInfoCombined(address user) external view returns (
        uint256[4] memory partA,
        uint256[4] memory partB
    ) {
        partA = [
            userStakedTokens[user],
            userStakeTime[user],
            userMiningSlotsCompleted[user],
            userTotalMiningRewards[user]
        ];

        partB = [
            userTotalWithdrawn[user],
            userMonth12HoldingBalance[user],
            userHoldingRewardsClaimed[user],
            currentPhase
        ];

        return (partA, partB);
    }
}

// File: remix-backup-at-1h6min-2025-10-11 (1)/.workspaces/SHAGUN-Polygon-Deployment/SHAGUNViewsFinal.sol


  pragma solidity ^0.8.20;


  abstract contract SHAGUNViewsFinal is SHAGUNViewsMinimal {

      constructor(
          address _adminAddress,
          address _companyWallet,
          address _liquidityWallet,
          address initialOwner
      ) SHAGUNViewsMinimal(_adminAddress, _companyWallet, _liquidityWallet, initialOwner) {}

      // ABSOLUTELY MINIMAL getUserInfo - Uses named returns to avoid stack issues
      function getUserInfoFinal(address user) external view returns (
          uint256 stakedTokens,
          uint256 stakeTime,
          uint256 miningSlotsCompleted,
          uint256 totalRewards,
          uint256 totalWithdrawn,
          uint256 month12HoldingBalance,
          uint256 holdingRewardsClaimed,
          uint256 currentPhase_
      ) {
          // Assign to named return variables - no local variables = no stack pressure
          stakedTokens = userStakedTokens[user];
          stakeTime = userStakeTime[user];
          miningSlotsCompleted = userMiningSlotsCompleted[user];
          totalRewards = userTotalMiningRewards[user];
          totalWithdrawn = userTotalWithdrawn[user];
          month12HoldingBalance = userMonth12HoldingBalance[user];
          holdingRewardsClaimed = userHoldingRewardsClaimed[user];
          currentPhase_ = currentPhase;
      }

      // Individual getter functions (ultra-safe, 1 value each)
      function getStakedTokens(address user) external view returns (uint256) {
          return userStakedTokens[user];
      }

      function getStakeTime(address user) external view returns (uint256) {
          return userStakeTime[user];
      }

      function getMiningSlotsCompleted(address user) external view returns (uint256) {
          return userMiningSlotsCompleted[user];
      }

      function getTotalMiningRewards(address user) external view returns (uint256) {
          return userTotalMiningRewards[user];
      }

      function getTotalWithdrawn(address user) external view returns (uint256) {
          return userTotalWithdrawn[user];
      }

      function getMonth12HoldingBalance(address user) external view returns (uint256) {
          return userMonth12HoldingBalance[user];
      }

      function getHoldingRewardsClaimed(address user) external view returns (uint256) {
          return userHoldingRewardsClaimed[user];
      }

      // Simple getter for just the phase number (doesn't conflict with parent)
      function getPhaseNumber() external view returns (uint256) {
          return currentPhase;
      }

      // Batch getters for related data (2 values each, maximum safety)
      function getStakingInfoFinal(address user) external view returns (uint256 staked, uint256 stakeTime) {
          return (userStakedTokens[user], userStakeTime[user]);
      }

      function getMiningInfoFinal(address user) external view returns (uint256 slots, uint256 rewards) {
          return (userMiningSlotsCompleted[user], userTotalMiningRewards[user]);
      }

      function getBalanceInfo(address user) external view returns (uint256 withdrawn, uint256 holding) {
          return (userTotalWithdrawn[user], userMonth12HoldingBalance[user]);
      }

      function getRewardsInfo(address user) external view returns (uint256 holding, uint256 claimed) {
          return (userMonth12HoldingBalance[user], userHoldingRewardsClaimed[user]);
      }
  }
// File: remix-backup-at-1h6min-2025-10-11 (1)/.workspaces/SHAGUN-Polygon-Deployment/SHAGUNViewsUltraSafe.sol


  pragma solidity ^0.8.20;


  abstract contract SHAGUNViewsUltraSafe is SHAGUNViewsFinal {

      constructor(
          address _adminAddress,
          address _companyWallet,
          address _liquidityWallet,
          address initialOwner
      ) SHAGUNViewsFinal(_adminAddress, _companyWallet, _liquidityWallet, initialOwner) {}

      // ULTRA-SAFE VIEW FUNCTIONS - MAXIMUM COMPILER SAFETY

      // Individual atomic getters (1 value each - bulletproof)
      function getUserStakedTokens(address user) external view returns (uint256) {
          return userStakedTokens[user];
      }

      function getUserStakeTime(address user) external view returns (uint256) {
          return userStakeTime[user];
      }

      function getUserMiningSlots(address user) external view returns (uint256) {
          return userMiningSlotsCompleted[user];
      }

      function getUserMiningRewards(address user) external view returns (uint256) {
          return userTotalMiningRewards[user];
      }

      function getUserTotalWithdrawn(address user) external view returns (uint256) {
          return userTotalWithdrawn[user];
      }

      function getUserHoldingBalance(address user) external view returns (uint256) {
          return userMonth12HoldingBalance[user];
      }

      function getUserHoldingClaimed(address user) external view returns (uint256) {
          return userHoldingRewardsClaimed[user];
      }

      // Safe batch getters (2 values each) - renamed to avoid conflicts
      function getStakingInfoUltra(address user) public view returns (uint256 staked, uint256 stakeTime) {
          return (userStakedTokens[user], userStakeTime[user]);
      }

      function getMiningInfoUltra(address user) public view returns (uint256 slots, uint256 rewards) {
          return (userMiningSlotsCompleted[user], userTotalMiningRewards[user]);
      }

      function getWithdrawalInfo(address user) public view returns (uint256 withdrawn, uint256 claimed) {
          return (userTotalWithdrawn[user], userHoldingRewardsClaimed[user]);
      }

      // Simple phase number getter (doesn't conflict with parent's full getCurrentPhase)
      function getContractPhase() public view returns (uint256) {
          return currentPhase;
      }

      // ULTRA-SAFE MAIN GETTER - SPLIT TO AVOID ALL STACK ISSUES
      function getUserInfoUltraSafe(address user) external view returns (
          uint256 stakedTokens,
          uint256 stakeTime,
          uint256 miningSlotsCompleted,
          uint256 totalRewards,
          uint256 totalWithdrawn,
          uint256 month12HoldingBalance,
          uint256 holdingRewardsClaimed,
          uint256 currentPhase_
      ) {
          // Read all values into memory using named return variables
          stakedTokens = userStakedTokens[user];
          stakeTime = userStakeTime[user];
          miningSlotsCompleted = userMiningSlotsCompleted[user];
          totalRewards = userTotalMiningRewards[user];
          totalWithdrawn = userTotalWithdrawn[user];
          month12HoldingBalance = userMonth12HoldingBalance[user];
          holdingRewardsClaimed = userHoldingRewardsClaimed[user];
          currentPhase_ = currentPhase;

          // Return values are already assigned to named return variables
      }
  }
// File: remix-backup-at-1h6min-2025-10-11 (1)/.workspaces/SHAGUN-Polygon-Deployment/SHAGUN.sol


  pragma solidity ^0.8.20;


  contract SHAGUN is SHAGUNViewsUltraSafe {

      constructor(
          address _adminAddress,
          address _companyWallet,
          address _liquidityWallet,
          address initialOwner
      ) SHAGUNViewsUltraSafe(_adminAddress, _companyWallet, _liquidityWallet, initialOwner) {}

      // Concrete implementation - all functionality inherited from parent contracts
      // This contract is deployable and removes the 'abstract' keyword
  }